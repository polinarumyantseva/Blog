import { logout, autorize, register, fetchRoles, fetchUsers, updateUserRole, removeUser } from './operations';

export const server = {
	logout,
	autorize,
	register,
	fetchRoles,
	fetchUsers,
	updateUserRole,
	removeUser,
};
